# frozen_string_literal: true

RSpec.describe Quizzly do
  it "has a version number" do
    expect(Quizzly::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
